#!/bin/bash

package=$1
test=$2
times=$3
/home/ashwin/solana/cargo nightly build --package "$package" --tests
for i in $(seq "$times"); do
  echo "run $i"
  /home/ashwin/solana/cargo nightly test --package "$package" "$test" -- --exact --nocapture --test-threads=1 > flaky.txt 2>&1 || exit 0
done
